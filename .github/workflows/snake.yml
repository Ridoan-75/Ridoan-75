name: Generate Contribution Snake

on:
  push:
    paths:
      - 'README.md'
      - '.github/workflows/snake.yml'
  schedule:
    - cron: '30 18 * * 0'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: Platane/snk@v3
        with:
          github_user_name: Ridoan-75
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=ef4444&color_dots=%23fecaca,%23fca5a5,%23f87171,%23dc2626,%23991b1b
            dist/github-contribution-grid-snake-dark.svg?color_snake=ef4444&color_dots=%23fecaca,%23fca5a5,%23f87171,%23dc2626,%23991b1b
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: crazy-max/ghaction-github-pages@v4
        with:
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}